class AppVectors {
  static final basePath = "assets/vectors/";

  static final ext = ".svg";

  static final achievement_onboarding = "${basePath}achievement_onboarding$ext";
  static final today_task_1_onboarding =
      "${basePath}today_task_1_onboarding$ext";
  static final today_task_onboarding = "${basePath}today_task_onboarding$ext";
  static final working_level_onboarding =
      "${basePath}working_level_onboarding$ext";
  static final working_period_onboarding =
      "${basePath}working_period_onboarding$ext";

  static final workmate_logo =
      "${basePath}workmate_logo$ext";

       static final work_profile_arrow =
      "${basePath}work_profile_arrow$ext";
  
  static final work_profile_logo =
      "${basePath}work_profile_logo$ext";

  static final clock_in_out_logo =
      "${basePath}clock_in_logo$ext";

      static final clock_logo =
      "${basePath}clock$ext";

      static final task_progress_code =
      "${basePath}task_progress_code$ext";

      static final task_progress_time =
      "${basePath}task_progress_time_yellow$ext";

      static final task_progress_done =
      "${basePath}task_progress_done$ext";

      static final task_progress_yellow_face =
      "${basePath}yellow_face$ext";

      static final expense_summary_logo =
      "${basePath}expense_summary_logo$ext";

      static final expense_summary_total =
      "${basePath}expense_summary_total$ext";

      static final expense_status_date =
      "${basePath}expense_status_date_logo$ext";
}
