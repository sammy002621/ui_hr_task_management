import 'package:flutter/material.dart';

class TaskProgressPage extends StatelessWidget {
  const TaskProgressPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("task progress page"),
      ),
    );
  }
}